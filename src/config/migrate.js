// MongoDB with Mongoose does not require migrations.
// Collections and indexes are created automatically by Mongoose models.
// Run `npm run db:seed` to populate initial data.
console.log('ℹ️  No migrations needed — Mongoose manages the schema automatically.');
process.exit(0);
